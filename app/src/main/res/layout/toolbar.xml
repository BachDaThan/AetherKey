<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageButton
        android:id="@+id/btn_translate_screen"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/toolbar_translate_screen"
        android:src="@android:drawable/ic_menu_search"
        android:tint="@color/aether_secondary"
        android:scaleType="centerInside"
        android:padding="10dp" />

    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_weight="1" />

    <ImageButton
        android:id="@+id/btn_translate_text"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/toolbar_translate_text"
        android:src="@android:drawable/ic_menu_send"
        android:tint="@color/aether_accent"
        android:scaleType="centerInside"
        android:padding="10dp" />

    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_weight="1" />

    <ImageButton
        android:id="@+id/btn_settings"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/toolbar_settings"
        android:src="@android:drawable/ic_menu_preferences"
        android:tint="@color/aether_primary"
        android:scaleType="centerInside"
        android:padding="10dp" />

</merge>
